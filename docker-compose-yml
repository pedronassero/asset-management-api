version: '3'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
    depends_on:
      - db
    restart: always

  db:
    image: mysql:5.7
    environment:
      MYSQL_DATABASE: assetsmanagement
      MYSQL_ROOT_PASSWORD: gekAipn10
    volumes:
      - db_data:/var/lib/mysql
    restart: always

volumes:
  db_data:
